---

---

<section class="hero">
  <div class="container hero-content">
    <span class="badge">Astro 5.0 Powered Portfolio</span>
    <h1>
      Dijital & Fiziksel <br />
      <span class="text-gradient">Dünyalar Üretiyorum</span>
    </h1>
    <p>
      Merhaba, ben <strong>İsmet ALP</strong>. Oyun geliştirmeden heykel
      sanatına, 3D modellemeden yazılım mimarisine uzanan geniş bir yelpazede
      eserler üretiyorum. Teknolojiyi ve sanatı birleştirerek benzersiz
      deneyimler tasarlıyorum.
    </p>
    <div class="cta-group">
      <a href="/projects" class="btn">Projelerimi Gör</a>
      <a href="#features" class="btn btn-secondary">Teknolojiyi Keşfet</a>
    </div>
  </div>
  <div class="hero-bg"></div>
</section>

<style>
  .hero {
    position: relative;
    padding: 8rem 0;
    overflow: hidden;
    display: flex;
    align-items: center;
  }

  .hero-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    background: radial-gradient(
      circle at 50% 50%,
      rgba(139, 92, 246, 0.15) 0%,
      transparent 50%
    );
    pointer-events: none;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    max-width: 800px;
  }

  .badge {
    display: inline-block;
    padding: 0.5rem 1rem;
    border-radius: 100px;
    background: rgba(139, 92, 246, 0.1);
    color: var(--color-primary);
    font-size: 0.85rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
    border: 1px solid rgba(139, 92, 246, 0.2);
  }

  h1 {
    font-size: 4rem;
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .text-gradient {
    background: linear-gradient(
      to right,
      var(--color-secondary),
      var(--color-accent)
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  p {
    font-size: 1.25rem;
    margin-bottom: 2.5rem;
    max-width: 600px;
  }

  .cta-group {
    display: flex;
    gap: 1rem;
  }

  @media (max-width: 768px) {
    .hero {
      padding: 4rem 0;
    }

    h1 {
      font-size: 2.5rem;
    }

    .cta-group {
      flex-direction: column;
    }
  }
</style>
